# Build output
/dist/
/bin/
*.test
# coverage report artifacts (anchored / specific so they don't match source
# files like internal/mcov/coverage.go)
/coverage.out
coverage.html
coverage.txt
*.lcov

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code — personal/machine-local permissions (keep settings.json committed)
.claude/settings.local.json
